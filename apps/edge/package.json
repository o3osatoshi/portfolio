{
  "name": "@repo/edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "deploy:prv": "wrangler versions upload",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@repo/interface": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.45.3"
  },
  "engines": {
    "node": ">=22"
  }
}
