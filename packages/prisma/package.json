{
  "name": "@repo/prisma",
  "version": "0.0.0",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "prisma db seed",
    "env:pull:dev": "doppler secrets download --project portfolio-prisma --config dev --no-file --format env > ./.env.development.local",
    "env:pull:lcl": "doppler secrets download --project portfolio-prisma --config lcl --no-file --format env > ./.env.test.local",
    "env:pull:prd": "doppler secrets download --project portfolio-prisma --config prd --no-file --format env > ./.env.production.local",
    "format": "prisma format",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "migrate:reset": "prisma migrate reset",
    "migrate:status": "prisma migrate status",
    "studio": "prisma studio",
    "test": "vitest run",
    "test:cvrg": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@o3osatoshi/toolkit": "workspace:*",
    "@prisma/adapter-pg": "catalog:",
    "@prisma/client": "catalog:",
    "@repo/domain": "workspace:*",
    "neverthrow": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@o3osatoshi/config": "workspace:*",
    "@testcontainers/postgresql": "^11.10.0",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "dotenv": "catalog:",
    "npm-run-all": "catalog:",
    "prisma": "catalog:",
    "testcontainers": "^11.10.0",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": "22.x"
  }
}
