/**
 * @packageDocumentation
 * Edge HTTP adapter surface (`buildEdgeApp`, `buildEdgeHandler`).
 */
export { buildEdgeApp, buildEdgeHandler } from "./app";
export type { EdgeAppType, EdgeDeps } from "./app";
